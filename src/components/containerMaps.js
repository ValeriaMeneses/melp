import React, { Component } from 'react';

export default class ContainerMaps extends Component{
  render(){
    let address = this.props.address
    // console.log('adress:::::', address);
    return(
      <div className="containerMap">
      </div>
    )
  }
}
